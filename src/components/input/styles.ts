import { createStyles } from "@mantine/core"

export const useStyles = createStyles(() => ({
  form: {
    position: "relative",
  },
  button: {
    position: "absolute",
    top: "16%",
    right: ".4em",
    width: "83px",
    height: "32px",
}
}));